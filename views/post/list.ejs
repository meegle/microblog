<% list.forEach(function(info, index) { %>
	<% if (index % 3 == 0) { %>
	<div class="row">
	<% } %>
		<div class="span4">
			<h2><a href="/u/<%= info.user %>"><%= info.user %></a>&nbsp;说</h2>
			<p><small><%= info.time %></small></p>
			<p><%= info.post %></p>
		</div>
	<% if (index % 3 == 2) { %>
	</div><!-- end row -->
	<% } %>
<% }) %>
<% if (list.length % 3 != 0) { %>
	</div><!-- end row -->
<% } %>